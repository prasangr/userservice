/*
package org.example.userservice.security.repository;

import org.example.userservice.security.models.Client;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

@Repository
public class ClientRepository implements JpaRepository<Client, String> {
    // This interface will automatically provide CRUD operations for the Client entity
    // You can add custom query methods if needed
    Optional<Client> findByClientId(String clientId);
}
*/
